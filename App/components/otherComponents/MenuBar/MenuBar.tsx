"use client"

import { styled } from "styled-components"

const MenuBar = () => {
 return (
    <MenuBarContainer>
        <div>This is side menu bro</div>
    </MenuBarContainer>
 )
}

const MenuBarContainer = styled.section`
    display: flex;
`

export default MenuBar