apiVersion: v2
name: upstat
description: Deploys the full Upstat stack — api-common (Go, gRPC + HTTP), api-observability (Python), web (Next.js), and the Envoy gRPC-Web proxy.
type: application
version: 0.2.1
appVersion: "0.1.0"
